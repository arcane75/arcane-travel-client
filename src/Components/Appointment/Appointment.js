import React from 'react';
import Header from '../Header/Header';

const Appointment = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Appointment;